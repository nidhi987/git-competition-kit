# Challenge 1: Two Sum

Write a function `two_sum(nums, target)` that returns a tuple `(i, j)` of
indices such that `nums[i] + nums[j] == target`, with `i < j`.

Submit your solution at: `solutions/challenge-1/solution.py`

Your file must define a function with this exact signature:

```python
def two_sum(nums: list[int], target: int) -> tuple[int, int]:
    ...
```
